<!-- Copyright (c) 2026 FanXeon@Poemcoder with Codex -->

# 路线图

[完整产品交付计划](PRODUCT_DELIVERY_PLAN.md) 定义从当前 alpha 到普通用户可交付版本的分阶段工作、自定义快捷键合同和验收门禁；本文保留简明功能清单。Codex CLI 之后的方向——agent 状态回传、多 agent CLI、按键宏、以及桌面副屏（墨水屏 / 开发板）的设备无关协议——见 [AI Coding 桌面控制台计划](AGENT_DESK_PLAN.md)。

**当前阶段：V2 / 0.2.2，P2 / P3 代码收口，P4 / P5 验收中。** 设备选择、重连、按键热更新、高亮、单次测试、导入导出、原子安装回滚和模型校验已实现；安装版登录启动的重新登录、长时间和第二设备真机验收仍待执行。最新边界见 [开发进度快照](DEVELOPMENT_STATUS.md)。

## 0.1 硬件打通

- [x] macOS CoreBluetooth 扫描和 GATT 枚举
- [x] ATVV v0.4/v1.0 与 ADPCM
- [x] 本地 Whisper 和 Codex 提交
- [x] App Bundle、诊断和权限入口
- [x] 06「中心连接」正式 Logo、AppIcon、单色模板和概念证据归档
- [x] 脱敏扫描报告、完整 GATT 枚举与原始通知帧采集
- [x] 小米蓝牙遥控器 2 Pro 配对与 GATT 实测
- [ ] 真实按住、松手、静音和断连测试（按住/松手已通过）
- [x] 真实中文语音端到端提交

## 0.2 日常使用

- [x] 首次设置向导：macOS、Whisper/模型、辅助功能、蓝牙、Codex 输入区和安全启动组件六项真实检查
- [x] App 内置签名运行根：安装后启停、按键门禁、映射恢复和语音引擎修复不依赖源码目录原路径
- [x] 点击菜单栏图标打开轻量 GUI：搜索、连接、就绪、录音、后台转写、发送和错误状态；聚焦 Codex、打开记录、设置诊断与安全退出
- [x] 菜单栏实体指令反馈：常态使用 macOS 原生黑白模板，按真实动作短暂切换单色图标；无彩色圆角底，语音断连不阻断按键反馈
- [x] 后台启动/停止、重复实例门禁和退出时设备专属映射恢复
- [x] 串行后台转写与提交队列；允许继续录下一条，最多一条处理中和一条等待中
- [x] 剪贴板 change-count 并发保护，以及录音目录 `0700` / 语音产物 `0600`
- [x] 设备选择与设备标识持久化；多设备确定性仲裁、“随时就绪 / 智能休眠”双模式恢复、按键事件唤醒与手动重试
- [x] Preferences v3：schema、原子写入、`0700/0600`、损坏隔离、v1/v2 迁移、语音连接模式热更新和未来版本保护
- [x] 登录启动使用 `SMAppService.mainApp` 并显示关闭、启用、需允许和不可用真实状态
- [ ] 登录启动在安装版完成启用、注销登录/重新登录和 ad-hoc 更新后的真机验收
- [x] GUI 可切换自动发送 / 仅转写，以及启用 / 关闭实体按键控制
- [x] 版本化用户预设：录制标准键盘快捷键、TV 目标配置、原子保存、选择、删除与危险快捷键拒绝
- [x] 按键与快捷键 GUI：独立 Tab、动作选择、快捷键录制、保存/选择与可达文字列表
- [x] 用户预设导入/导出、官方默认回退、真实按键高亮、单次测试和跨进程热重载
- [x] 安装前拒绝运行中 App，分期签名校验、原子替换与失败回滚
- [x] Whisper 模型固定 SHA-256 完整性校验与原子替换
- [x] `learn-buttons` 真机按键学习、单键复测与脱敏 HID Usage 报告
- [x] `debug-buttons` 人工确认校准、重测/跳过控制与预设动作预览
- [x] 小米 2 Pro 十二个接管键逐项独立复测并建立固件 2671 键码表（方向四键、确认、返回、HOME、TV、电源、语音和音量加减均已按新格式确认；菜单沿用 macOS 原生鼠标右键）
- [x] 设备事件 → 物理按钮 → 可替换预设 → 执行器的通用映射层
- [ ] MI-AO 命令面板：方向键选择、确认执行、返回取消
- [x] 双控制模式实现：`TV` 仅切换方向环的鼠标移动 / 上下左右；确认和返回固定为 Return / Escape
- [x] 电源键启动或聚焦 Codex（实现完成，Keyboard Power `0x07/0x66` 真机证据完成）
- [x] 十二个接管键到 HID `No Event` 的设备专属映射；菜单沿用 macOS 原生鼠标右键；v1/v2/v3 迁移、一键恢复和真机 Usage 保留验证
- [x] HOME 单击 Page Down / 350 ms 内双击 Page Up 的延迟仲裁，确保双击不先触发 Page Down
- [x] 音量加减完成 `0x80` / `0x81` 人工确认，并映射到 Codex 上一个 / 下一个会话
- [x] 空映射门禁、未知用户映射拒绝、所有权状态与卸载恢复自动测试
- [x] 指针模式长按加速曲线和按键松开停止；移除会误拦截 Mac 实体键盘的全局事件过滤器
- [x] 完成方向四键、确认、返回六项新格式真机校准
- [x] 四个方向完成 HID 识别、`No Event` 隔离、直接光标定位和真实坐标变化的真机闭环
- [ ] 完成 TV 模式切换和电源动作的逐项真机验收
- [ ] 多显示器边界、同时使用 Mac 键盘和长时间连续按压压力测试

## 1.0

- [ ] 至少两类真实语音遥控器兼容矩阵
- [ ] 稳定的源码构建发布；Developer ID 二进制作为可选通道
- [ ] 稳定升级和迁移策略
- [ ] 隐私、安全和失败路径审查
